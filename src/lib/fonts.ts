import { Newsreader } from "next/font/google";

export const newsreaderNormal = Newsreader({
  weight: ['300'],
  style: 'normal',
  subsets: ['latin'],
  display: 'swap',
});

export const newsreaderItalic = Newsreader({
  weight: ['300'],
  style: 'italic',
  subsets: ['latin'],
  display: 'swap',
});